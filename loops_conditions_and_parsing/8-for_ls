#!/usr/bin/env bash
# This script displays the content of the current directory, removing the part before the first dash

for file in *; do
    # Check if the file contains a dash
    if [[ $file == *-* ]]; then
        # Remove everything before the first dash
        echo "${file#*-}"
    else
        # Print the filename as is
        echo "$file"
    fi
done

